 
 -- revisão no modelo anterior da máquina e implementação do montador.
 -- nenhuma alteração no arquivo base do motandor, só inclusão de includes e dados que estavam privados. de resto, a estrutura e funcionalidades são as mesmas.
 -- ainda não foi testado, esperando processador de macros para gerar arquivos para serem expandidos, possiveis bugs.
 -- precisamos definir algumas coisas ainda, mas já coloquei a base para os proximos checkpoints.
 -- arquivos base, design, estrutura e representação de caracteres na interface grafica.
